# M01 完整 Agent 评测证据

更新日期：2026-08-11

## `agent-eval-m01-v1`

- 范围：仅 `M01:initial`，关闭 thinking、subagent、skill 和 mutation。
- 结果：`graph_recursion_error`，`0 succeeded / 1 failed`，没有最终文本、Token 回执或可做业务评审的答案。
- 时长：3.5 秒；本地密封目录为 `.deer-flow/incubation-agent-eval/agent-eval-m01-v1/`，保持 gitignore。
- 轨迹：只观察到第一次 `incubation_project_context` 工具意图，没有工具结果；因此不能判断项目事实、外部证据、方法检索或孵化结论质量。

## 根因

运行器把 `--max-agent-steps 12` 当作足够的业务步数，但该参数实际是 LangGraph 图超步上限。当前 DeerFlow Lead Agent 编译图有 25 个节点；一次正常的模型、工具和最终回答还要经过多个 before/after middleware 节点，12 连首个工具往返都无法完成。

轨迹收集器还会把同一流式工具调用的首个空参数块当成完整事件，丢弃后续参数增量。这没有造成图超步，但会让审计轨迹错误显示 `{}`。

## 修正与边界

- 先增加失败测试，再要求图超步上限处于 `40..100`；下一次单例重跑使用 50。
- 同一 call ID 的流式工具参数改为合并，密封轨迹保留最终完整脱敏参数。
- trial 上限仍为 1；DeerFlow 重复工具检测仍在第 3 次警告、第 5 次硬停。
- 本次不计为模型技术成功，也不计为宝妈案例业务失败；修正提交且工作区重新干净后，才允许用新 run ID 重跑一次。

## `agent-eval-m01-v2`

- 范围仍只有 `M01:initial`；图超步上限提高到 50，其他开关不变。
- 结果仍为 `graph_recursion_error`，运行 79.9 秒，没有最终答案。
- Agent 成功依次读取 `incubation_project_context`、`incubation_project_evidence` 和 `incubation_context`；三项都有工具结果，事实、证据及方法边界保持分离。
- 第四个模型决定是调用 `write_file` 生成交付物，尚未取得工具结果就耗尽 50 图超步。它不是重复工具循环，而是 DeerFlow 通用提示词要求正式交付物写入文件，与本次只需评审聊天文本的目标冲突。
- 失败运行没有 `end` 事件，旧收集器因此丢失已经发生的部分 Token；DeerFlowClient 的流式事件也只发出首个空参数块，虽然工具实际以完整参数成功执行，密封调用仍显示 `{}`。

## 第二次修正

- 评测请求明确只在对话中回答，不创建或呈现文件；这只约束交付格式，不规定孵化路线或工具顺序。
- 图超步上限改用生产默认量级 100，同时新增独立 `--max-model-calls 6` 硬上限；后者才是本次付费调用次数护栏。
- DeerFlowClient 在终态 values 快照发现参数比流式块完整时补发工具调用更新；轨迹按 call ID 合并。
- 轨迹从每个 AI 消息累计 usage，因此即使没有最终 `end` 事件也保留部分 Token。
- `v2` 同样不进入业务评分。上述修正通过测试并提交、工作区重新干净前，不执行 `v3`。

## `agent-eval-m01-v3`

- 范围仍只有 `M01:initial`，关闭 thinking、subagent、skill 和 mutation；上限为 1 个 trial、100 个图超步和 6 次模型调用。
- 运行模型：`doubao-seed-2-0-pro-260215`。
- 技术结果：`1 succeeded / 0 failed`，时长 66.841 秒；输入 `43,527`、输出 `2,209`、合计 `45,736` Token。
- 轨迹：先后读取项目事实和项目证据，随后同一轮发出两次语义等价的 `incubation_context` 查询，最后直接在聊天中回答。工具参数已完整密封，没有文件写入、项目变更或发布动作。
- 完整性：`manifest.json`、`agent-manifest.json`、SQLite 状态库、trace 和 records 的本地 SHA-256 均与旁置摘要匹配；证据目录为 `.deer-flow/incubation-agent-eval/agent-eval-m01-v3/`，保持 gitignore。

## 人工业务评审

结论：**技术通过，业务不通过。**

通过项：

- 保留了十年企业会计、每周八小时、不展示孩子、不承诺投资收益、财务体检和月度顾问等主要事实与限制。
- 将内容、私信承接、财务体检和月度顾问连接为初步变现路径，并列出未知和替代形式。
- 没有声称保证爆款、保证成交或补造具体价格。

拒绝原因：

1. 无来源地断言小红书“小生意创业者、个体户聚集，财务需求明确”，并直接把它定为首发平台；现有官方小红书来源只描述 MCN 合作能力，不支持该受众和平台效果判断。
2. 把“真实企业财务案例”和可安全公开的案例供给当成现有资产。十年会计经验不能证明拥有可发布案例、客户授权或小生意案例。
3. 未询问镜头意愿就选择口播、上半身或手部电脑演示，又无依据地决定“不提宝妈身份”；这些可以是待测选项，不能写成已经匹配主体的路线。
4. 自行设定四周、每周两条、平均播放 500、三条意向评论、五条服务私信等数字。方法卡已经明确“没有基线时先采集范围，不编造行业阈值”，评测请求也禁止补造指标阈值。
5. 声称以上任一流量或互动指标达成就“说明需求存在”，混淆平台注意力、受众兴趣、咨询意向和真实付费需求；未达成时笼统调整内容方向，也没有隔离实验变量。
6. 声称十年经验足以让内容供给“完全可持续”，超过了当前证据。可持续素材来源、脱敏边界、税务建议资质和实际制作耗时仍未知。

## 对架构的含义

`v3` 证明同一个 DeerFlow Lead Agent 能读取类型隔离的事实、证据和按需方法并完成回答，但也证明**外挂知识存在不等于模型会遵守知识**：它在同一回答里直接违反了已读取的方法反例。因此 ADR-007 继续 `proposed`，本次不能支持架构胜者。

下一次付费重跑前应先离线验证两个问题：压缩重复方法结果以降低上下文和费用；在不增加固定孵化流程或语义硬门的前提下，让最终建议清楚标记“有来源观察、Agent 推断、实验选择与未知”，尤其禁止把任意数字包装成需求证据。

## A31 离线修正结果

- 已把真实宽查询加入第 13 条检索难例。默认有界检索现在返回定位、表现形式、内容发动机、变现、转化和实验设计，不再返回与本判断无关的小红书 MCN 来源。
- 小红书官方来源明确增加不可外推范围：不能据此判断受众构成、品类需求、项目的平台适配或平台优先级。
- 表现形式、内容发动机和实验设计卡增加本次失败对应的来源化反例，分别约束镜头意愿、可公开案例/持续素材和需求信号层级。
- 核心孵化提示词保持不变；没有新增第二 Agent、固定流程、语义中间件、输出改写或营销评分门。
- 聚焦离线测试为 `8 passed`。新单次方法结果更完整，旧运行中的语义重复调用是否消失仍须由下一次真实轨迹观察。
- 本节没有付费模型调用。M01 仍是业务拒绝，只能标记为“离线修正已测试”，不能标记为“宝妈孵化通过”。详见 `../audits/A31-m01-grounding-and-retrieval-repair.md`。

## `agent-eval-m01-v4`

- 用户明确同意后，以相同 M01 和 `doubao-seed-2-0-pro-260215` 进行一次受限付费复测；上限仍为 1 个 trial、100 个图超步和 6 次模型调用。
- 技术结果：`1 succeeded / 0 failed`，23.589 秒；输入 `27,899`、输出 `728`、合计 `28,627` Token。trace 与 preflight ledger 均完整通过验真。
- 工具面包含三项孵化工具，但实际轨迹只读取项目事实和项目证据，没有调用 `incubation_context`，因此 A31 修正后的六卡检索没有进入模型上下文。
- 业务仍不通过：回答新增露脸口播、粉丝群、免费资料、99/299 元定价、服务交付范围、四周八条、双平台、三咨询阈值和“宝妈家庭理财/兼职报税”替代业务。
- 本次证明按需方法检索修正是必要但不充分的；不能通过强制工具调用或继续堆案例提示词解决。完整评审见 `../audits/A33-m01-paid-retest-v4.md`。

## A34 主体适配与记忆隔离复核

- 用户业务评审指出，露脸口播本身高度同质化且依赖主体表现力；在不了解主体真实表达、证明、素材和生产条件前，不能把它从候选提升为主方案。
- v3 与 v4 使用不同 actor、thread、project 和状态库；两个运行 actor 的默认 Agent DeerMem 上下文均为 0 字符，详见 `2026-08-11-m01-memory-isolation-check.json`。相似结论来自相同稀疏输入下的模型默认套路，不是旧答案回灌。
- 旧评测仍未显式关闭宿主记忆开关，因此实现已改为使用独立 `AppConfig` 副本，同时关闭注入和写入。真实产品配置及项目事实账本不受影响。
- 薄合同只增加“最少反转信息”和“表现形式须有主体适配证据”两项认识边界；表现形式方法卡升级为 v4。4000 字上下文预算回归仍保留受控案例，没有用放宽预算掩盖提示词膨胀。
- 本轮只有离线测试，没有付费模型调用。M01 继续是 `business-rejected / offline-repair-tested`，详见 `../audits/A34-subject-fit-and-memory-isolation.md`。
